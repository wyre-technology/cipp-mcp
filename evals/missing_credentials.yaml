name: cipp_list_tenants returns is_error when credentials missing
server:
  command: node
  args: [dist/entry.js]
  env:
    AUTH_MODE: "static"
    CIPP_BASE_URL: ""
    CIPP_API_KEY: ""
assert:
  tool: cipp_list_tenants
  args: {}
  expect:
    is_error: true
timeout: 30s
